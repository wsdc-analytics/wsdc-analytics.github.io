# Archive Scripts

Deprecated or one-off investigative scripts are stored here.

Rules:
- do not use archived scripts in release workflow,
- keep for traceability only,
- migrate reusable logic back into `scripts/pipeline` or `scripts/maintenance`.
